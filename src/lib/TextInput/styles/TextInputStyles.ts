import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    fontSize: 14,
  },
  container: {
    marginBottom: 32,
    width: '100%',
    height: 40,
  },

  inputIcon: {
    paddingRight: 10,
  },
});
